# All API calls proxied through frontend nginx/CRA — no cross-origin needed
# Only set REACT_APP_COCKPIT_URL if overriding the default (auto-detected from browser hostname)
# REACT_APP_COCKPIT_URL=http://localhost:5174
